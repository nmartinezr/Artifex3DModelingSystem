# Geometry Tests

Property-oriented tests for deterministic geometry behavior.

Representative assertions include watertightness, manifold status, positive volume, expected component count, bounding boxes, transforms and absence of unexpected intersections.
