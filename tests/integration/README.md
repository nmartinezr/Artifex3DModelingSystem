# Integration Tests

Cross-module workflows such as:

```text
Image → Provider → Project Model → Validation → Export → Reload → Validate
```

Tests should assert semantic geometry/project properties rather than only transport success.
