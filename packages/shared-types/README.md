# Shared Types

Small cross-cutting types that are safe to share across modules without importing runtime implementations.

This package must remain dependency-light and must not become a dumping ground for service-specific models.
