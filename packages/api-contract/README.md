# API Contract

Versioned application-facing request/response/event contracts shared between API consumers and implementations.

Contracts should reference large binary artifacts through asset/resource identifiers and provide stable machine-readable error codes.
